package exchange.core2.core.simulation;

/**
 * Operations measured by the production simulation.
 */
public enum SimulationOperation {
    SUBMIT_LIMIT,
    SUBMIT_PROTECTED,
    REPLACE,
    CANCEL,
    CHECKPOINT
}
